{
  "name": "marvel-experience-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Marvel Experience mobile app built from the existing web app.",
  "scripts": {
    "sync:android": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0",
    "@capacitor/ios": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0",
    "typescript": "^5.8.3"
  }
}
